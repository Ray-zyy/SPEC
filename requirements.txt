numpy>=1.26
scipy>=1.11
pandas>=2.1
pyarrow>=14
scikit-learn>=1.3
matplotlib>=3.8
tqdm
tabulate
shap                      # 可选, E3(e) SHAP; 缺失时自动退回置换重要性
pytest
# GPU: 2080Ti (sm_75) 用 CUDA 12.1 wheel
# pip install torch --index-url https://download.pytorch.org/whl/cu121
